class Article {
  constructor(
    id,
    title,
    imageUrl,
    text,
    date,
  ) {
    this.id = id;
    this.title = title;
    this.imageUrl = imageUrl;
    this.text = text;
    this.date = date;
  }
}

export default Article;
